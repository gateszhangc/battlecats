apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: battlecats-preview
  annotations:
    nginx.ingress.kubernetes.io/force-ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
    - host: battlecats.144.91.77.245.sslip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: battlecats
                port:
                  number: 80
